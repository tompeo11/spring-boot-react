export default interface Product {
    id: number
    name: string
    description: string
    unitPrice: number
    imageUrl: string
    unitsInStock: number
    brand: string
    categoryName: string
}